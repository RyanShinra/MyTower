
class PersonProtocol(Protocol):
  
    @property
    def current_floor_float(self) -> Blocks: ... rename to current_vertical_position
    
    @property
    def current_block_float(self) -> Blocks: ... rename to current_horizontal_position
    
    
    @property
    def destination_block_num(self) -> Blocks: ... rename to destination_horizontal_position
    

    def set_destination(self, dest_floor_num: int, dest_block_num: Blocks) -> None: ... change to def set_destination(self, dest_floor_num: int, dest_horiz_pos: Blocks)
    

class ElevatorProtocol(Protocol):
    """Protocol defining the interface for Elevator entities"""
    
    
    @property
    def state(self) -> ElevatorState: ... rename to elevator_state
       
    @property
    def fractional_floor(self) -> Blocks: ... rename to vertical_position
    
    @property
    def current_block_float(self) -> Blocks: ... rename to horizontal_position
    

class ElevatorTestingProtocol(Protocol):
    """Testing-only protocol for Elevator - provides internal state access for unit tests"""
    
    def testing_set_state(self, state: ElevatorState) -> None: ... rename to testing_set_state
    
    def testing_set_current_floor(self, floor: Blocks) -> None: ... rename to testing_set_current_vertical_pos
    

class ElevatorBankProtocol(Protocol):
    """Protocol defining the interface for ElevatorBank entities"""
    
    @property
    def horizontal_block(self) -> Blocks: ... rename to horizontal_position
    
    def get_waiting_block(self) -> Blocks: ... rename to get_waiting_position


class FloorProtocol(Protocol):
    """Protocol defining the interface for Floor entities"""
    
    @property
    def width(self) -> Blocks: ... rename to floor_width
    
    @property
    def height(self) -> Blocks: ... rename to floor_height
    

class BuildingProtocol(Protocol):
    """Protocol defining the interface for Building entities"""
 
    @property
    def floor_width(self) -> Blocks: ... rename to building_width
    
    @property
    def people(self) -> List[PersonProtocol]: ...  delete this property and associated members
        
    def add_person(self, person: PersonProtocol) -> None: ... delete this property and associated members
    
    def remove_person(self, person_id: str) -> None: ...  delete this property and associated members
    


class PersonTestingProtocol(Protocol):
    """Testing-only protocol for Person - provides internal state access for unit tests"""
    
    
    def testing_confirm_dest_block_is(self, block: Blocks) -> bool: ... rename this to testing_confirm_horiz_dest_is
    
    
    def testing_set_current_floor_float(self, cur_floor: float) -> None: ... rename this to testing_set_current_vertical_position
    
    def testing_get_current_floor_float(self) -> float: ... rename this to testing_get_current_vertical_position
    
    def testing_set_current_block_float(self, cur_block: Blocks) -> None: ... rename this to testing_set_current_horiz_position
    